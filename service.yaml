apiVersion: v1
kind: Service
metadata:
  name: project2-app-service
spec:
  selector:
    app: project2-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort